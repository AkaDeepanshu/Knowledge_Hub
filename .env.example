# ====================================
# KNOWLEDGE HUB - Environment Variables
# ====================================
# Copy this file to 'backend/.env' and fill in your actual values

# ----------------
# Server Configuration
# ----------------
PORT=5000
NODE_ENV=development

# ----------------
# Database Configuration
# ----------------
# MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
# Get this from: https://cloud.mongodb.com/ → Connect → Connect your application
MONGO_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/knowledge_hub?retryWrites=true&w=majority

# Alternative: Local MongoDB
# MONGO_URI=mongodb://localhost:27017/knowledge_hub

# ----------------
# Authentication
# ----------------
# Secret key for JWT token signing (use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secret_jwt

# JWT expiration time (optional)
# JWT_EXPIRES_IN=7d

# ----------------
# AI/LLM Configuration
# ----------------
# Google Gemini AI API Key
# Get this from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_google_gemini_api_key_here

# LLM Model Selection (optional)
# GEMINI_MODEL=gemini-2.0-flash-exp

# Toggle to use mock LLM service (for testing without API calls)
# USE_MOCK_LLM=false

# ----------------
# Rate Limiting (optional)
# ----------------
# LLM endpoint rate limit (requests per window)
# LLM_RATE_LIMIT_MAX=5
# LLM_RATE_LIMIT_WINDOW_MS=900000  # 15 minutes

# Auth endpoint rate limit (requests per window)
# AUTH_RATE_LIMIT_MAX=10
# AUTH_RATE_LIMIT_WINDOW_MS=900000  # 15 minutes

# ----------------
# CORS Configuration (optional)
# ----------------
# Frontend URL for CORS
# FRONTEND_URL=http://localhost:3000

# ----------------
# Logging (optional)
# ----------------
# LOG_LEVEL=info

# ====================================
# SETUP INSTRUCTIONS
# ====================================
#
# 1. Copy this file:
#    - Windows: copy .env.example backend\.env
#    - Mac/Linux: cp .env.example backend/.env
#
# 2. Edit backend/.env with your actual values
#
# 3. REQUIRED variables (must be set):
#    - MONGO_URI
#    - JWT_SECRET
#    - GEMINI_API_KEY
#
# 4. OPTIONAL variables:
#    - All others have sensible defaults
#
# 5. For production deployment:
#    - Use strong JWT_SECRET (minimum 32 characters)
#    - Set NODE_ENV=production
#    - Use environment-specific MongoDB credentials
#    - Never commit .env file to version control
#
# ====================================
# GETTING API KEYS
# ====================================
#
# MongoDB Atlas (Free Tier):
# 1. Sign up: https://www.mongodb.com/cloud/atlas
# 2. Create cluster (M0 Free tier)
# 3. Database Access → Add user
# 4. Network Access → Add IP (0.0.0.0/0 for dev)
# 5. Connect → Connect your application → Copy URI
#
# Google Gemini AI:
# 1. Visit: https://aistudio.google.com/app/apikey
# 2. Sign in with Google account
# 3. Create API Key
# 4. Copy and paste here
# 5. Free tier: 15 requests/minute
#
# ====================================
